/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Disable Tailwind CSS integration
  experimental: {
    // Disable automatic Tailwind detection
  },
};

export default nextConfig;
